// Goal
// Source: /Docs/InitialDB-plan.md - "Goal" (inferred from the relationship map)

model Goal {
  id          String @id @default(uuid()) @db.Uuid
  ownerId     String @db.Uuid
  description String

  owner User @relation(fields: [ownerId], references: [id])

  @@index([ownerId])
}
